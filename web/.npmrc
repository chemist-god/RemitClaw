# thirdweb → valtio pins use-sync-external-store@1.2.0 (React 18 peers only).
# overrides in package.json lift it to 1.4.0 for React 19; this avoids hard install failures on CI.
legacy-peer-deps=true
